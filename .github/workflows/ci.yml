name: CI

on:
  pull_request:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  checks:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.11

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Fast checks
        run: bun run check

      - name: Client unit tests
        run: bun run client:test

      - name: Server tests
        run: bun run server:test

  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    needs: checks

    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.11

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Install Playwright browser
        run: bun x playwright install --with-deps chromium

      - name: Smoke tests
        run: bun run e2e:test

  docker_changes:
    runs-on: ubuntu-latest
    outputs:
      docker: ${{ steps.filter.outputs.docker }}

    steps:
      - uses: actions/checkout@v4

      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            docker:
              - 'Dockerfile'
              - 'compose.local.yml'
              - 'DEPLOY.md'
              - 'scripts/docker-local.sh'
              - '.github/workflows/ci.yml'

  docker_smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: [checks, smoke, docker_changes]
    if: github.ref == 'refs/heads/main' || needs.docker_changes.outputs.docker == 'true'

    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.11

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Install Playwright browser
        run: bun x playwright install --with-deps chromium

      - name: Build image
        run: bun run docker:build

      - name: Start local stack
        run: bun run docker:local-up

      - name: Docker smoke
        run: bun run docker:local-test

      - name: Docker UI smoke
        run: bun run docker:ui-smoke

      - name: Stop local stack
        if: always()
        run: bun run docker:local-down
