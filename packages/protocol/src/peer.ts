export type PeerColor = {
    background: string
    stroke: string
}

export type LocalPeer = {
    id: string
    name: string
    color: PeerColor
    textColor: string
    textColorLight: string
}
