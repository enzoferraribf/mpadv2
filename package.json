{
    "name": "mpad",
    "private": true,
    "packageManager": "bun@1.3.11",
    "workspaces": [
        "packages/client",
        "packages/server",
        "packages/core",
        "packages/protocol",
        "packages/text-core",
        "packages/testkit",
        "tools/legacy-import"
    ],
    "scripts": {
        "dev": "turbo dev",
        "build": "turbo build",
        "check": "biome check --formatter-enabled=true --organize-imports-enabled=true --linter-enabled=false . && biome lint . && turbo typecheck --filter=@mpad/client --filter=@mpad/server --filter=@mpad/core --filter=@mpad/protocol --filter=@mpad/testkit --filter=@mpad/text-core --filter=@mpad/legacy-import",
        "test": "bun run --cwd packages/client test && ./scripts/docker-test-db.sh bun run --cwd packages/server test",
        "smoke": "./scripts/run-playwright.sh --config playwright.config.ts",
        "docker:smoke": "./scripts/docker-local.sh smoke",
        "legacy:import": "bun run --cwd tools/legacy-import import"
    },
    "devDependencies": {
        "@biomejs/biome": "^1.9.4",
        "@playwright/test": "^1.54.2",
        "turbo": "^2.9.4",
        "typescript": "^5.9"
    }
}
