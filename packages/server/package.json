{
    "name": "@mpad/server",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "bun --env-file=../../.env run --watch src/index.ts",
        "migrate": "bun --env-file=../../.env run src/infrastructure/migrate.ts",
        "start": "bun --env-file=../../.env run src/index.ts",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "@mpad/core": "workspace:*",
        "@mpad/protocol": "workspace:*",
        "@mpad/text-core": "workspace:*",
        "yjs": "^13.6",
        "y-protocols": "^1.0",
        "lib0": "^0.2"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "typescript": "^5.9"
    }
}
